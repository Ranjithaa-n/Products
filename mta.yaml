_schema-version: "3.2"
ID: comproducts
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comproducts-destination-content
  type: com.sap.application.content
  requires:
  - name: comproducts-destination-service
    parameters:
      content-target: true
  - name: comproducts-repo-host
    parameters:
      service-key:
        name: comproducts-repo-host-key
  - name: comproducts-uaa
    parameters:
      service-key:
        name: comproducts-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comproducts_html_repo_host
          ServiceInstanceName: comproducts-html5-service
          ServiceKeyName: comproducts-repo-host-key
          sap.cloud.service: comproducts
        - Authentication: OAuth2UserTokenExchange
          Name: comproducts_uaa
          ServiceInstanceName: comproducts-xsuaa-service
          ServiceKeyName: comproducts-uaa-key
          sap.cloud.service: comproducts
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: comproducts-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comproducts-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comproducts.zip
      name: comproducts
      target-path: resources/
- name: comproducts
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comproducts-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comproducts-destination-service
    service-plan: lite
- name: comproducts-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: comproducts-xsuaa-service
    service-plan: application
- name: comproducts-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comproducts-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
